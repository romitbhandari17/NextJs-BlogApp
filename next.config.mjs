/** @type {import('next').NextConfig} */
const nextConfig = {
    // Add static site export settings
    // output: 'export',  // To export the site as static files
  
    // Optionally add a base path if your app is hosted in a subdirectory
    // basePath: '/my-app',
  
    // Add trailing slash to URLs (optional)
    trailingSlash: true,  // Adds a trailing slash to the end of URLs
  
    // Enable React strict mode
    // reactStrictMode: true,
    
    // Other configurations (if needed)
  };

export default nextConfig;
